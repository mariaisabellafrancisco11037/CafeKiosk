"use strict";
const express=require("express");const router=express.Router();const store=require("../services/promotionStore");const {activeNow}=require("../services/promotionEngine");let auth={};try{auth=require("../middleware/authMiddleware");}catch(_){auth={};}const verify=typeof auth.verifyToken==="function"?auth.verifyToken:(req,res,next)=>next();const admin=typeof auth.isAdmin==="function"?auth.isAdmin:(req,res,next)=>next();
router.get('/active',async(req,res,next)=>{try{const cafeId=String(req.query.cafeId||'cafe-1');const rows=(await store.list(cafeId)).filter(x=>activeNow(x));res.json({success:true,cafeId,count:rows.length,promotions:rows});}catch(e){next(e);}});
router.get('/',verify,admin,async(req,res,next)=>{try{const cafeId=String(req.user?.cafeId||'cafe-1');const rows=await store.list(cafeId);res.json({success:true,cafeId,count:rows.length,promotions:rows});}catch(e){next(e);}});
router.post('/',verify,admin,async(req,res,next)=>{try{const row=await store.upsert({...req.body,cafeId:req.user?.cafeId||'cafe-1'});res.status(201).json({success:true,promotion:row});}catch(e){next(e);}});
router.patch('/:id',verify,admin,async(req,res,next)=>{try{const cafeId=req.user?.cafeId||'cafe-1';const existing=(await store.list(cafeId)).find(x=>String(x.id)===String(req.params.id));if(!existing)return res.status(404).json({success:false,message:'Promotion not found.'});const row=await store.upsert({...existing,...req.body,id:existing.id,cafeId:existing.cafeId});res.json({success:true,promotion:row});}catch(e){next(e);}});
router.delete('/:id',verify,admin,async(req,res,next)=>{try{const cafeId=String(req.user?.cafeId||'cafe-1');const ok=await store.remove(req.params.id,cafeId);res.status(ok?200:404).json({success:ok,message:ok?'Promotion deleted.':'Promotion not found.'});}catch(e){next(e);}});
module.exports=router;
