const express=require('express');const router=express.Router();const pool=require('../config/dbPool');const {verifyToken,isAdmin}=require('../middleware/authMiddleware');
router.get('/',verifyToken,isAdmin,async(req,res,next)=>{try{const cafeId=String(req.query.cafeId||req.user?.cafeId||'cafe-1');const [[storeRows],[taxRows],[prefRows],[payRows]]=await Promise.all([pool.execute('SELECT * FROM store_settings WHERE cafe_id=?',[cafeId]),pool.execute('SELECT * FROM tax_settings WHERE cafe_id=?',[cafeId]),pool.execute('SELECT * FROM system_preferences WHERE cafe_id=?',[cafeId]),pool.execute('SELECT * FROM payment_methods WHERE cafe_id=? ORDER BY sort_order',[cafeId])]);const st=storeRows[0]||{},tx=taxRows[0]||{},pf=prefRows[0]||{};const enabled=new Map(payRows.map(x=>[x.method_name,Boolean(x.is_enabled)]));res.json({success:true,cafeId,settings:{cash:enabled.get('Cash')!==false,gcash:enabled.get('GCash')||false,card:enabled.get('Card')||false,other:enabled.get('Other')||false,storeName:st.store_name||'',address:st.address||'',contact:st.contact_number||'',email:st.email||'',open:st.opening_time?String(st.opening_time).slice(0,5):'',close:st.closing_time?String(st.closing_time).slice(0,5):'',tax:Number(tx.tax_rate_percent||0),service:Number(tx.service_charge_percent||0),defaultOrder:pf.default_order_type||'Dine In',lowStock:Number(pf.low_stock_warning_default||10),currencyCode:pf.currency_code||'PHP',currencySymbol:pf.currency_symbol||'₱'}});}catch(e){next(e);}});
router.put('/',verifyToken,isAdmin,async(req,res,next)=>{try{const cafeId=String(req.body?.cafeId||req.user?.cafeId||'cafe-1');const s=req.body?.settings||req.body||{};const conn=await pool.getConnection();try{await conn.beginTransaction();await conn.execute(`INSERT INTO store_settings (cafe_id,store_name,address,contact_number,email,opening_time,closing_time) VALUES (?,?,?,?,?,?,?) ON DUPLICATE KEY UPDATE store_name=VALUES(store_name),address=VALUES(address),contact_number=VALUES(contact_number),email=VALUES(email),opening_time=VALUES(opening_time),closing_time=VALUES(closing_time)`,[cafeId,s.storeName||'CafeKiosk',s.address||null,s.contact||null,s.email||null,s.open||null,s.close||null]);await conn.execute(`INSERT INTO tax_settings (cafe_id,tax_rate_percent,service_charge_percent) VALUES (?,?,?) ON DUPLICATE KEY UPDATE tax_rate_percent=VALUES(tax_rate_percent),service_charge_percent=VALUES(service_charge_percent)`,[cafeId,Number(s.tax||0),Number(s.service||0)]);await conn.execute(`INSERT INTO system_preferences (cafe_id,default_order_type,low_stock_warning_default,currency_code,currency_symbol) VALUES (?,?,?,?,?) ON DUPLICATE KEY UPDATE default_order_type=VALUES(default_order_type),low_stock_warning_default=VALUES(low_stock_warning_default),currency_code=VALUES(currency_code),currency_symbol=VALUES(currency_symbol)`,[cafeId,s.defaultOrder==='Take Out'?'Take Out':'Dine In',Number(s.lowStock||10),s.currencyCode||'PHP',s.currencySymbol||'₱']);const methods=[['Cash','Cash',s.cash!==false,1],['GCash','GCash / E-wallet',Boolean(s.gcash),2],['Card','Card',Boolean(s.card),3],['Other','Other',Boolean(s.other),4]];for(const m of methods)await conn.execute(`INSERT INTO payment_methods (cafe_id,method_name,display_name,is_enabled,sort_order) VALUES (?,?,?,?,?) ON DUPLICATE KEY UPDATE display_name=VALUES(display_name),is_enabled=VALUES(is_enabled),sort_order=VALUES(sort_order)`,[cafeId,...m]);await conn.commit();res.json({success:true,message:'Settings saved to MySQL.'});}catch(e){await conn.rollback();throw e;}finally{conn.release();}}catch(e){next(e);}});
module.exports=router;
